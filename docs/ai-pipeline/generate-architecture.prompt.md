---
agent: agent
description: 'Stage 1 — Architecture Discovery. Scans any repository and bootstraps the AI governance pipeline.'
tools:
  - semantic_search
  - grep_search
  - file_search
  - read_file
  - list_dir
  - create_file
  - replace_string_in_file
---

# AI Governance Bootstrap — Stage 1

You are an AI engineering assistant responsible for bootstrapping AI governance for this repository.

This repository may contain existing production code.
The repository itself is the source of truth.

Do NOT modify application source code.

---

# Important Rule

This prompt file must remain generic and static.

It must never modify itself.

Developers may update this file manually if governance philosophy changes.

All other governance files must be generated or updated based on this prompt.

---

# Governance Workflow

The repository governance system runs in two stages:

Stage 1 → Architecture Discovery
Stage 2 → Runtime Installation

Each stage must wait until the previous stage finishes successfully.

Outputs from each stage become inputs for the next stage.

All generated files must support upsert behavior:

- create files if missing
- update files if they exist
- preserve manual edits when possible

---

# Stage 1 — Architecture Discovery

Analyze the repository to understand the project architecture.

Scan the repository and detect:

- programming languages
- frameworks and libraries
- architecture patterns
- module or domain boundaries
- folder structure
- dependency direction
- APIs (REST, GraphQL, gRPC)
- databases
- messaging systems
- testing frameworks
- linting tools
- CI/CD workflows

Infer these from files such as:

package.json
nx.json
pom.xml
build.gradle
\*.csproj
requirements.txt
go.mod
Dockerfile
docker-compose.yml
.github/workflows

Do not assume the stack. Detect it from the repository.

---

# Generate Architecture Documentation

Create directory:

docs/architecture

Generate:

docs/architecture/overview.md
docs/architecture/modules.md

---

## overview.md

Summarize the repository architecture including:

- detected languages
- frameworks
- architecture pattern
- API style
- databases
- messaging systems
- testing frameworks
- CI/CD system

Include a tech stack table with versions detected from dependency files.

Include a project structure tree showing apps and libraries with their purpose.

Include a data flow section showing the request lifecycle from entry to persistence.

Include an infrastructure section covering database, caching, auth, and deployment.

Every generated file must include a provenance comment:

<!-- Auto-generated by generate-architecture.prompt.md — do not remove this line -->

---

## modules.md

Identify logical modules or domains in the repository.

For each module include:

- module name
- location in repository
- responsibilities
- dependency direction

Document dependency rules derived from actual imports.

Document shared concerns such as configuration, logging, validation, and error handling.

Example dependency flow:

controllers → services → repositories

---

# Generate Stage 2 Prompt

After architecture documentation is created, generate the next stage prompt:

docs/ai-pipeline/generate-copilot.prompt.md

The Stage 2 prompt must:

1. Have YAML frontmatter with agent: agent, a description, and tools list
2. Include a provenance comment identifying it was generated by this file
3. Read architecture documentation as its source of truth
4. Generate docs/architecture/context.md (compressed governance context)
5. Generate all 17 runtime files listed below

The Stage 2 prompt must treat these files as source of truth:

docs/architecture/overview.md
docs/architecture/modules.md

The Stage 2 prompt must derive governance dynamically from architecture instead of hardcoding framework assumptions.

Example logic:

If architecture indicates NestJS → enforce resolver/service layering
If architecture indicates Spring Boot → enforce controller/service/repository layering
If architecture indicates .NET → enforce controller/service/data-access layering

The context.md (placed in docs/architecture/) must be a compressed governance context (60–100 lines) with these sections:

- Identity
- Stack
- Architecture Pattern
- Request Lifecycle
- Module Map
- Dependency Rules
- Testing Strategy
- File Conventions
- CI/CD

The Stage 2 prompt must then generate exactly these runtime files:

AGENTS.md
.github/copilot-instructions.md
.github/instructions/architecture.instructions.md
.github/instructions/testing.instructions.md
.github/prompts/development.prompt.md
.github/prompts/refactor.prompt.md
.github/prompts/pr-review.prompt.md
.github/prompts/fix-pr-comment.prompt.md
.github/prompts/improve-coverage.prompt.md
.github/skills/development/SKILL.md
.github/skills/pr-review/SKILL.md
.github/skills/testing/SKILL.md
.github/agents/orchestrator.agent.md
.github/agents/planner.agent.md
.github/agents/coder.agent.md
.github/agents/tester.agent.md
.github/agents/reviewer.agent.md

---

# Stage 1 Completion Criteria

Stage 1 is complete only if these files exist:

docs/architecture/overview.md
docs/architecture/modules.md
docs/ai-pipeline/generate-copilot.prompt.md

Do NOT start Stage 2 until these files are generated.

After verifying all three files exist, state:

"Stage 1 complete. Run docs/ai-pipeline/generate-copilot.prompt.md to continue to Stage 2."
